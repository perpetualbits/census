# openldap fleet server — generated by fleet.sh confd
[server]
name         = "openldap-b"
host         = "localhost"
port         = 3391
use_ssl      = false
base_dn      = "dc=bravo,dc=test"
bind_dn      = "cn=admin,dc=bravo,dc=test"
password_cmd = "printf secret"
verify       = false
