# openldap fleet server — generated by fleet.sh confd
[server]
name         = "openldap-a"
host         = "localhost"
port         = 3390
use_ssl      = false
base_dn      = "dc=alpha,dc=test"
bind_dn      = "cn=admin,dc=alpha,dc=test"
password_cmd = "printf secret"
verify       = false
